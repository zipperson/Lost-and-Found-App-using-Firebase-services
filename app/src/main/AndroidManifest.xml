<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ACTUALLOSTOFUND">

        <!-- SPLASH (ONLY LAUNCHER) -->
        <activity
            android:name=".ui.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- LOGIN -->
        <activity
            android:name=".ui.LoginActivity"
            android:exported="false" />

        <!-- SIGNUP -->
        <activity
            android:name=".ui.SignupActivity"
            android:exported="false" />

        <!-- MAIN -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="false" />

        <!-- PROFILE / AVATAR PICKER -->
        <activity
            android:name=".ui.ProfileSetupActivity"
            android:exported="false" />

        <!-- SUBMIT LOST ITEM -->
        <activity
            android:name=".ui.SubmitLostItemActivity"
            android:exported="false" />

        <!-- QUESTIONS -->
        <activity
            android:name=".ui.QuestionsActivity"
            android:exported="false"/>

        <activity
            android:name=".ui.AnswersActivity"
            android:exported="false"/>

        <!-- FCM -->
        <service
            android:name=".ui.MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>
</manifest>
